# Benchmark Project

Run the example experiment:

```bash
python main.py -p experiment/toy/toydata_linear_toy_reproduce.yaml
```

## Live Dashboard

A static dashboard is available at `webapp/benchmark-live/`.

- Local use: serve repo root with a static server and open `/webapp/benchmark-live/`
- GitHub Pages deployment workflow: `.github/workflows/deploy-benchmark-live.yml`
